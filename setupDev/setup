#!/usr/bin/env bash
set -euo pipefail

. "$scripts/installer.sh"

install curl
$setupDev/chrome
$setupDev/vsCode
$setupDev/speedTest
$setupDev/remoteContainers
$setupDev/spellChecker
$setupDev/git
$setupDev/gh
$setupDev/docker
$setupDev/rustDesk
$setupDev/anyDesk
install zip
install unzip
install simplescreenrecorder
install nginx
install telnet
$setupDev/mkcert
$setupDev/micro
install trans translate-shell
$setupDev/beyondCompare
exit
install sshd openssh-server
install http httpie
install pip python3-pip
install nmap
install jq
install cloc
install progress
install rename
install parallel
install baobab
install dconf-editor
# $setupDev/dnsmasq
$setupDev/safeEyes
# $setupDev/stretchly
# $setupDev/warpCli
# $setupDev/bashCompletion
$setupDev/azureCli
$setupDev/rClone
$setupDev/node
# bash "$(dirname "$0")/npmPackages"
sudo homeDir="$homeDir" node "$setupDev/nginx"
# bash "$(dirname "$0")/adsPower"
# bash "$(dirname "$0")/astrill"
