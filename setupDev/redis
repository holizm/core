#!/usr/bin/env bash

. "$scripts/installer.sh"

installRedis() {
    sudo apt-get update -y
    sudo apt-get install -y redis-server
    sudo systemctl enable redis-server
}

install redis installRedis
