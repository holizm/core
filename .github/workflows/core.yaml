name: core

on:
  workflow_dispatch:
  schedule:
    - cron: '0 23 * * 5'
  push:
    branches:
      - main
    paths:
      - 'container/files/dev/core'

jobs:
  build:
    uses: ./.github/workflows/builder.yaml
    with:
      service: core
    secrets: inherit
