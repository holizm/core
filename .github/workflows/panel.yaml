name: panel

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * 6'
  push:
    branches:
      - main
    paths:
      - 'container/files/dev/panel'

jobs:
  build:
    uses: ./.github/workflows/builder.yaml
    with:
      file: panel
    secrets: inherit
