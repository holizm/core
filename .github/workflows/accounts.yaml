name: accounts

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * 6'
  push:
    branches:
      - main
    paths:
      - 'container/files/prod/accounts'

jobs:
  build:
    uses: ./.github/workflows/builder.yaml
    with:
      file: accounts
      dir: prod
    secrets: inherit
