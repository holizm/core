name: site

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * 6'
  push:
    branches:
      - main
    paths:
      - 'container/files/dev/site'

jobs:
  build-site:
    uses: ./.github/workflows/builder.yaml
    with:
      service: site
    secrets: inherit
