services:
    search:
        image: getmeili/meilisearch:latest
        container_name: ${repo}SearchDatabases
        hostname: search
        ports:
            - 127.0.0.1:${databaseSearchPort}:7700
        environment:
            - MEILI_MASTER_KEY=IC1joYMLZTT0fQMJ5qcz
        volumes:
            - /var/tmp/${repo}/search/databases:/meili_data
        restart: always
