name: ${process}

on:
    workflow_dispatch:

env:
    GH_TOKEN: ${{ secrets.PAT }}
    isCiCd: true

jobs:
    buildAndPush:
        permissions: write-all
        runs-on: ubuntu-latest
        steps:
