name: ${process}

on:
    workflow_dispatch:

env:
    GH_TOKEN: ${{ secrets.PAT }}
    isCiCd: true
    process: ${process}

jobs:
    buildAndPush:
        permissions: write-all
        runs-on: ubuntu-latest
        steps:
