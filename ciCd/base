name: ${process}

on:
    workflow_dispatch:

env:
    GH_TOKEN: ${{ secrets.PAT }}

jobs:
    buildAndPush:
        permissions: write-all
        runs-on: ubuntu-latest
        steps:

            - name: Clone holizm
              if: startsWith(runner.name, 'GitHub')
              run: |
                    clone holizm
