
            - name: Start the container
              run: |
                    echo $HOME
                    ls -lah /home
                    ls -lah $HOME
                    cd ~/${repo}/${process}
                    start
                    echo "process=$process" >> $GITHUB_ENV
