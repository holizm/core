# Timeless Developer Interface

Developers should focus on product work rather than infrastructure technology. Development tooling must expose common, durable, semantically correct terms that require no specialized teaching.

Use `start` and `stop` as the primary project lifecycle commands. Keep container, network, certificate, dependency, and composition details behind this interface.
