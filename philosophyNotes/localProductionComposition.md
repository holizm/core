# Local Production Composition

Starting a project locally must reproduce the meaningful production composition of its APIs, databases, panels, sites, and supporting services.

Map production domains predictably to local domains by replacing the top-level domain with `.local`, such as `holism.com` becoming `holism.local`. Configure local DNS automatically and provide locally trusted development certificates so developers can use HTTPS without browser security warnings.
