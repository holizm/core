# Standardized Source Composition

The platform is a collection of separate repositories rather than a monorepo. Every developer clones the repositories into the same standardized paths and works with the same filesystem composition as every other team member.

Development composition does not depend on pull-request merges or publishing platform-owned libraries to NPM or another package host. Compose places the source repositories directly beside one another so the runnable project uses local platform code at development time.
